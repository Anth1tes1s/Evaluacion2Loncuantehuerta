import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RecuperarcontrasniaPage } from './recuperarcontrasnia.page';

describe('RecuperarcontrasniaPage', () => {
  let component: RecuperarcontrasniaPage;
  let fixture: ComponentFixture<RecuperarcontrasniaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RecuperarcontrasniaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
